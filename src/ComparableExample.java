public class ComparableExample {

    // mostly used for sorting and comparing, LibraryApp.Book.java is where the comparetor function is.
   /* @Override
    public int compareTo(LibraryApp.Book o) {                          //under comparable
        if(bookid == o.bookid) return 0;                    //this goes under book, currently no valid variables to use this
        else if(bookid > o.bookid) return 1;
        else return -1;
    }*/
}
